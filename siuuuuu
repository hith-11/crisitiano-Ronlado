{
  "name": "cr7-premium-tribute",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "15.0.3",
    "react": "19.0.0-rc-66238b97-20241112",
    "react-dom": "19.0.0-rc-66238b97-20241112",
    "framer-motion": "^11.11.17",
    "lucide-react": "^0.460.0",
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.5.4"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "eslint": "^8",
    "eslint-config-next": "15.0.3"
  }
}import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./components/**/*.{js,ts,jsx,tsx,mdx}",
    "./app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        gold: {
          50: '#fbf8eb',
          100: '#f4edcc',
          200: '#e9da9b',
          300: '#dbbf61',
          400: '#d4af37', // Primary Gold
          500: '#c5a028',
          600: '#a37a1e',
          700: '#825c1c',
          800: '#6d4a1d',
          900: '#5e401e',
        },
        cr7red: "#E11D48",
        dark: "#0A0A0A",
      },
      backgroundImage: {
        'glass-gradient': 'linear-gradient(rgba(255, 255, 255, 0.05), rgba(255, 255, 255, 0.01))',
      }
    },
  },
  plugins: [],
};
export default config;
export const STATS = [
  { label: "Total Goals", value: "900+", category: "Career" },
  { label: "Champions League", value: "5", category: "Trophies" },
  { label: "Ballon d'Or", value: "5", category: "Awards" },
  { label: "Apps", value: "1200+", category: "Career" },
];

export const CAREER_TIMELINE = [
  {
    year: "2002-2003",
    club: "Sporting CP",
    description: "The beginning of a legend. Debuted at 17.",
    achievements: ["Portuguese Super Cup"]
  },
  {
    year: "2003-2009",
    club: "Manchester United",
    description: "Transformation into a world-class player under Sir Alex Ferguson.",
    achievements: ["Premier League (3)", "UCL (1)", "Ballon d'Or"]
  },
  {
    year: "2009-2018",
    club: "Real Madrid",
    description: "Peak dominance. All-time leading scorer for the club.",
    achievements: ["UCL (4)", "La Liga (2)", "4 Ballon d'Or"]
  },
  {
    year: "2018-2021",
    club: "Juventus",
    description: "Conquering Italy and breaking Serie A records.",
    achievements: ["Serie A (2)", "Coppa Italia"]
  },
  {
    year: "2023-Present",
    club: "Al Nassr",
    description: "Globalizing football in the Middle East.",
    achievements: ["Arab Club Champions Cup"]
  }
];

export const TROPHIES = [
  { name: "UEFA Champions League", count: 5, icon: "🏆", color: "gold" },
  { name: "Premier League", count: 3, icon: "🦁", color: "blue" },
  { name: "La Liga", count: 2, icon: "🛡️", color: "yellow" },
  { name: "Euro Championship", count: 1, icon: "🇪🇺", color: "red" },
  { name: "Ballon d'Or", count: 5, icon: "⚽", color: "gold" },
];
"use client";
import { useState, useEffect } from "react";
import Link from "next/link";
import { motion, AnimatePresence } from "framer-motion";
import { Menu, X, Trophy } from "lucide-react";

const navLinks = [
  { name: "Home", href: "/" },
  { name: "Bio", href: "/biography" },
  { name: "Stats", href: "/statistics" },
  { name: "Timeline", href: "/timeline" },
  { name: "Gallery", href: "/gallery" },
  { name: "Contact", href: "/contact" },
];

export default function Navbar() {
  const [isOpen, setIsOpen] = useState(false);
  const [scrolled, setScrolled] = useState(false);

  useEffect(() => {
    const handleScroll = () => setScrolled(window.scrollY > 50);
    window.addEventListener("scroll", handleScroll);
    return () => window.removeEventListener("scroll", handleScroll);
  }, []);

  return (
    <nav className={`fixed w-full z-50 transition-all duration-300 ${scrolled ? "bg-black/90 backdrop-blur-md py-4 shadow-2xl" : "bg-transparent py-6"}`}>
      <div className="max-w-7xl mx-auto px-6 flex justify-between items-center">
        <Link href="/" className="flex items-center space-x-2 group">
          <span className="text-2xl font-black tracking-tighter text-white group-hover:text-gold-400 transition-colors">
            CRISTIANO <span className="text-gold-400">7</span>
          </span>
        </Link>

        {/* Desktop Nav */}
        <div className="hidden md:flex space-x-8">
          {navLinks.map((link) => (
            <Link key={link.name} href={link.href} className="text-sm font-medium text-gray-300 hover:text-gold-400 transition-colors uppercase tracking-widest">
              {link.name}
            </Link>
          ))}
        </div>

        {/* Mobile Toggle */}
        <button className="md:hidden text-white" onClick={() => setIsOpen(!isOpen)}>
          {isOpen ? <X size={28} /> : <Menu size={28} />}
        </button>
      </div>

      {/* Mobile Menu */}
      <AnimatePresence>
        {isOpen && (
          <motion.div 
            initial={{ opacity: 0, y: -20 }}
            animate={{ opacity: 1, y: 0 }}
            exit={{ opacity: 0, y: -20 }}
            className="absolute top-full left-0 w-full bg-black border-b border-white/10 p-6 flex flex-col space-y-4 md:hidden"
          >
            {navLinks.map((link) => (
              <Link key={link.name} href={link.href} onClick={() => setIsOpen(false)} className="text-xl font-bold text-white hover:text-gold-400">
                {link.name}
              </Link>
            ))}
          </motion.div>
        )}
      </AnimatePresence>
    </nav>
  );
}
"use client";
import { motion } from "framer-motion";
import { ArrowRight, Trophy, Target, Zap } from "lucide-react";

export default function Hero() {
  return (
    <section className="relative h-screen w-full flex items-center overflow-hidden bg-dark">
      {/* Background Graphic */}
      <div className="absolute inset-0 z-0">
        <div className="absolute inset-0 bg-gradient-to-r from-black via-black/80 to-transparent z-10" />
        <img 
          src="https://images.unsplash.com/photo-1599408162172-669fa979aa7f?auto=format&fit=crop&q=80&w=2000" 
          alt="CR7 Hero"
          className="w-full h-full object-cover opacity-60"
        />
      </div>

      <div className="relative z-20 max-w-7xl mx-auto px-6 w-full">
        <motion.div 
          initial={{ opacity: 0, x: -50 }}
          animate={{ opacity: 1, x: 0 }}
          transition={{ duration: 0.8 }}
          className="max-w-2xl"
        >
          <span className="text-gold-400 font-bold tracking-[0.3em] uppercase mb-4 block">The Greatest Of All Time</span>
          <h1 className="text-7xl md:text-9xl font-black text-white mb-6 leading-none">
            SIUUU<span className="text-gold-400">M</span>
          </h1>
          <p className="text-xl text-gray-400 mb-8 max-w-lg leading-relaxed">
            Five-time Ballon d'Or winner, record breaker, and the ultimate athlete. 
            Explore the legacy of Cristiano Ronaldo dos Santos Aveiro.
          </p>

          <div className="flex flex-wrap gap-4">
            <button className="bg-gold-400 hover:bg-gold-500 text-black px-8 py-4 rounded-full font-bold flex items-center transition-all transform hover:scale-105">
              EXPLORE CAREER <ArrowRight className="ml-2" size={20} />
            </button>
            <button className="bg-white/10 hover:bg-white/20 text-white px-8 py-4 rounded-full font-bold backdrop-blur-md border border-white/20 transition-all">
              WATCH HIGHLIGHTS
            </button>
          </div>
        </motion.div>
      </div>

      {/* Floating Stats */}
      <div className="absolute bottom-12 right-12 hidden lg:flex space-x-8">
        {[ { label: "Goals", val: "900+" }, { label: "Trophies", val: "33" } ].map((s, i) => (
          <motion.div 
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: i * 0.2 }}
            key={s.label} 
            className="text-right"
          >
            <div className="text-4xl font-black text-white">{s.val}</div>
            <div className="text-gold-400 text-sm font-bold uppercase tracking-widest">{s.label}</div>
          </motion.div>
        ))}
      </div>
    </section>
  );
}
import type { Metadata } from "next";
import { Inter, Montserrat } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";

const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });
const montserrat = Montserrat({ subsets: ["latin"], weight: ["900"], variable: "--font-montserrat" });

export const metadata: Metadata = {
  title: "Cristiano Ronaldo | The GOAT Official Tribute",
  description: "Official-style portfolio of Cristiano Ronaldo. Records, Stats, Gallery, and Career Journey.",
  openGraph: {
    images: ["/og-image.jpg"],
  },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="dark scroll-smooth">
      <body className={`${inter.variable} ${montserrat.variable} bg-dark text-white font-sans antialiased`}>
        <Navbar />
        <main>{children}</main>
        <footer className="py-12 border-t border-white/10 bg-black text-center">
          <p className="text-gray-500 text-sm">© 2024 CR7 Tribute. All rights reserved. Designed for excellence.</p>
        </footer>
      </body>
    </html>
  );
}
"use client";
import { motion } from "framer-motion";
import { BarChart3, Globe, Zap, Target } from "lucide-react";

const detailedStats = [
  { title: "Club Goals", value: "760+", icon: <BarChart3 className="text-gold-400" /> },
  { title: "International Goals", value: "130+", icon: <Globe className="text-gold-400" /> },
  { title: "Champions League Goals", value: "140", icon: <Target className="text-gold-400" /> },
  { title: "Assists", value: "250+", icon: <Zap className="text-gold-400" /> },
];

export default function StatisticsPage() {
  return (
    <div className="pt-32 pb-24 bg-dark min-h-screen">
      <div className="max-w-7xl mx-auto px-6">
        <header className="mb-20 text-center">
          <motion.h1 
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            className="text-6xl font-black mb-4 uppercase"
          >
            By The <span className="text-gold-400">Numbers</span>
          </motion.h1>
          <p className="text-gray-400 max-w-2xl mx-auto">
            Breaking records isn't the goal, it's the result of hard work. 
            A deep dive into the historical numbers of CR7.
          </p>
        </header>

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
          {detailedStats.map((item, idx) => (
            <motion.div 
              key={idx}
              initial={{ opacity: 0, scale: 0.9 }}
              whileInView={{ opacity: 1, scale: 1 }}
              transition={{ delay: idx * 0.1 }}
              className="p-10 bg-white/5 border border-white/10 rounded-3xl relative overflow-hidden"
            >
              <div className="mb-6 bg-gold-400/10 w-16 h-16 rounded-2xl flex items-center justify-center">
                {item.icon}
              </div>
              <div className="text-4xl font-black mb-2">{item.value}</div>
              <div className="text-gray-400 font-bold uppercase text-xs tracking-widest">{item.title}</div>
            </motion.div>
          ))}
        </div>

        {/* Placeholder for interactive chart area */}
        <div className="mt-20 p-12 bg-white/5 rounded-3xl border border-white/10 text-center italic text-gray-500">
          [Interactive Career Goals Distribution Chart Loading...]
        </div>
      </div>
    </div>
  );
}
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --background: #0a0a0a;
  --foreground: #ffffff;
}

body {
  color: var(--foreground);
  background: var(--background);
  font-family: var(--font-inter), sans-serif;
}

h1, h2, h3, h4 {
  font-family: var(--font-montserrat), sans-serif;
  letter-spacing: -0.05em;
}

.glass {
  background: rgba(255, 255, 255, 0.03);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.1);
}

::selection {
  background: #d4af37;
  color: #000;
}
